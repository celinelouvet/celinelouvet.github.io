import { type ThemingConfig } from '@chakra-ui/react';

import { timelineSlotRecipe } from './Timeline.slot-recipe';

export const slotRecipe = {
  timeline: timelineSlotRecipe,
} as ThemingConfig['slotRecipes'];
