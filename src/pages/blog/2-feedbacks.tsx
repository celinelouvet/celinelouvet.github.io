import { PageContainer } from '@/components/core';
import { FeedbackContent } from '@/components/features/blog/articles';

export default function Page() {
  return (
    <PageContainer>
      <FeedbackContent />
    </PageContainer>
  );
}
