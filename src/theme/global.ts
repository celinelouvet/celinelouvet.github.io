import { type GlobalStyles } from '@chakra-ui/theme-tools';

const nunito = 'Nunito';

export const global = {
  body: {
    fontFamily: nunito,
    fontSize: '14px',
    fontWeight: '400',
  },
} satisfies GlobalStyles['global'];
